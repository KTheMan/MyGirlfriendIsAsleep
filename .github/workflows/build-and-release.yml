name: Build and Release

on:
  push:
    tags:
      - 'v*'

jobs:
  build:
    name: Build
    strategy:
      matrix:
        platform: [windows, ubuntu, macos]
    runs-on: ${{ matrix.platform }}

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.x'

    - name: Convert icon
      run: |
        if [ "${{ matrix.platform }}" == "windows" ]; then
          magick -density "%[fx:96*256/50]" -background none icon.svg icon.ico
        elif [ "${{ matrix.platform }}" == "macos" ]; then
          brew install svg2png libicns
          svg2png -w 256 -h 256 ./icon.svg icon.png
          png2icns icon.icns icon.png
        fi
      shell: bash

    - name: Install and build with PyInstaller
      run: |
        if [ "${{ matrix.platform }}" == "windows" ]; then
          ICON_OPTION="--icon=icon.ico"
        elif [ "${{ matrix.platform }}" == "macos" ]; then
          ICON_OPTION="--icon=icon.icns"
        else
          ICON_OPTION=""
        fi
        pip install pyinstaller
        pyinstaller --onefile $ICON_OPTION mygirlfriendisasleep.py

    - name: Upload artifacts
      uses: actions/upload-artifact@v3
      with:
        name: mygirlfriendisasleep-${{ matrix.platform }}
        path: dist/mygirlfriendisasleep*

  release:
    name: Release
    needs: build
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Download artifacts
      uses: actions/download-artifact@v3

    - name: Extract changes for the current release from CHANGELOG.md
      id: changelog
      run: |
        CHANGELOG_CONTENT=$(awk -v version=${{ github.ref }} 'BEGIN {printmode=0; changelog=""} $0 ~ version {printmode=1} printmode {changelog=changelog"\n"$0} $0 ~ "^## " && $0 !~ version {printmode=0} END {print changelog}' CHANGELOG.md)
        echo "::set-output name=content::$CHANGELOG_CONTENT"

    - name: Create Release and Upload Artifacts
      uses: gh-release/gh-release-action@v1
      with:
        token: ${{ secrets.GITHUB_TOKEN }}
        tag_name: ${{ github.ref }}
        release_name: MyGirlfriendIsAsleep ${{ github.ref }}
        body: ${{ steps.changelog.outputs.content }}
        draft: false
        prerelease: false
        files: |
          ./mygirlfriendisasleep-windows/mygirlfriendisasleep-${{ github.ref }}.exe
          ./mygirlfriendisasleep-ubuntu/mygirlfriendisasleep-${{ github.ref }}
          ./mygirlfriendisasleep-macos/mygirlfriendisasleep-${{ github.ref }}
